<form method="post" action="{{ path('app_setting_delete', {'id': app_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app_setting.id) }}">
    <button class="btn">Delete</button>
</form>
