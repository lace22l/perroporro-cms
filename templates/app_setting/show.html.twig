{% extends 'base.html.twig' %}

{% block title %}AppSetting{% endblock %}

{% block body %}
    <h1>AppSetting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ app_setting.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ app_setting.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ app_setting.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_setting_index') }}">back to list</a>

    <a href="{{ path('app_setting_edit', {'id': app_setting.id}) }}">edit</a>

    {{ include('app_setting/_delete_form.html.twig') }}
{% endblock %}
