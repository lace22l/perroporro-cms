{% extends 'base_admin.html.twig' %}

{% block title %}InfoCard{% endblock %}

{% block body %}
    <h1>InfoCard</h1>

    <table class="table table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ info_card.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ info_card.title }}</td>
            </tr>
            <tr>
                <th>CardBody</th>
                <td>{{ info_card.cardBody }}</td>
            </tr>
            <tr>
                <th>ColSize</th>
                <td>{{ info_card.colSize }}</td>
            </tr>
            <tr>
                <th>Ordering</th>
                <td>{{ info_card.ordering }}</td>
            </tr>
            <tr>
                <th>ColOffset</th>
                <td>{{ info_card.colOffset }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_info_card_index') }}">back to list</a>

    <a href="{{ path('app_info_card_edit', {'id': info_card.id}) }}">edit</a>

    {{ include('info_card/_delete_form.html.twig') }}
{% endblock %}
