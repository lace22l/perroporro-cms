<form method="post" action="{{ path('app_info_card_delete', {'id': info_card.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_card.id) }}">
    <button class="btn">Delete</button>
</form>
